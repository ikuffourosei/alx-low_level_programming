#include "main.h"
#include <string.h>

/**
 * _strlen - returns length of string
 * @s: parameter to be checked
 * Return: 1 (success)
 */
int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
