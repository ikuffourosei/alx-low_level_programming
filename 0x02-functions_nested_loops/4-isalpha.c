#include "main.h"
#include <ctype.h>

/**
 *_isalpha - checks if an input is a alphabet
 * @c: parameter to be checked
 *
 *Return: 1 (true) and 0 (otherwise)
 */
int _isalpha(int c)
{
	return (isalpha(c));
}
