#include <stdio.h>
/*
 * main function is the entry point
 *
 * puts returns positive integer for succes, negative for failure
 */
int main() 
{
	const char *message = "\"Programming is like building a multilingual puzzle";

	puts(message);

	return(0);
}
