#include "main.h"

/**
 * mul -  multiply two integers
 * @a: first parameter to be checked
 * @b: second parameter to be checked
 *
 *Return: a * b
 */
int mul(int a, int b)
{
	int m;

	m = a * b;
	return (m);
}
